{
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "bash scripts/run-refresh.sh --scheduled",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
